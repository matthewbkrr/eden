# eden — сводка ревью (Phase 4, консолидация)

Дата: **2026-07-03** · Сводит три трека в одну картину: **auth-дизайн**
(`docs/adr/0002-auth-model.md`), **архитектура**
(`docs/review/architecture-review-2026-07-03.html`), **безопасность**
(`docs/review/security-findings-2026-07-03.md`). Ниже — только то, что видно
**на пересечении** треков, и предлагаемый список issues.

---

## Одна картина

Кодовая база **здоровая**: доменный слой — глубокие модули с чистыми швами,
авторизация через `%Scope{}` повсеместна и подтверждена аудитом, автоматика
безопасности зелёная, P0/P1 в auth/injection/session — ноль. Весь реальный
долг и риск концентрируется в **двух местах**, и оба видны сразу из нескольких
треков:

1. **Медиа-пайплайн на request-пути** — где сходятся *архитектура* и *безопасность*.
2. **`chat_live.ex` (13k строк)** — где сходятся *архитектура* и операционный риск
   (оба недавних P0 жили там).

Плюс **auth-трек** зафиксировал направление (ADR-0002) — и часть его решений
совпадает с security-находками, что даёт бесплатную синергию.

---

## Пересечения (это и есть ценность консолидации)

### ⨯1 · Медиа-декод: архитектура × безопасность × деплой
- **Security** (P1-1/2/3): тяжёлый декод/ffprobe/сжатие идут **синхронно на
  процессе LiveView**, пиксельные лимиты щедрые → инсайдер роняет ноду по OOM/CPU.
- **Архитектура** (S3): при этом Storage — образцовый шов, а `:media`-воркер уже
  существует и bounded (concurrency 5, таймаут 20 с). То есть **инфраструктура для
  фикса уже на месте** — надо лишь перенести синхронные стадии в неё.
- **Деплой** (память проекта): прод — single-VPS, уже был disk-full-инцидент
  2026-06-29. OOM здесь — не теоретический availability-риск.
- **Вывод:** одна правка (увести декод/shell-out с request-пути в `:media` + ужать
  лимиты) закрывает три P1 сразу и опирается на уже готовый шов. **Топ-приоритет.**

### ⨯2 · PubSub-централизация: архитектура × уже принятый ADR-0001
- **Архитектура** (S5): fan-out рассылки размазан по 21+ точке, «кого уведомлять»
  реализовано дважды (`direct_recipient_ids` / `thread_recipient_ids`).
- **ADR-0001** (notifications) уже решил вынести это в контекст `Eden.Notifications`
  с Web-адаптером как первым шагом.
- **Вывод:** не отдельная работа — **первый шаг ADR-0001 и есть централизация**.
  Делать вместе.

### ⨯3 · Пароль/сессии: безопасность × auth-ADR-0002
- **Security** (P2-2): нет смены пароля, нет «выйти на других устройствах»,
  60-дневные токены без ротации.
- **ADR-0002** уже закладывает reset-ссылки (Decision 6) и TOTP админам (Decision 7)
  на той же токен-машинерии.
- **Архитектура** (W2): третий токен-флоу (reset) — сигнал вынести общий
  `Eden.Tokens`-паттерн.
- **Вывод:** смена пароля + отзыв всех сессий + reset-ссылки + общий token-модуль —
  **один заход**, а не четыре.

### ⨯4 · Централизация видимости: архитектура × будущая org-фаза
- **Архитектура** (S4): фильтр видимости не полностью централизован (поиск
  пересобирает вручную) — единственный найденный путь к утечке данных.
- **Org-фаза** (#172): `org_id` добавится именно через этот фильтр; если он не
  единый, забытый `where org_id` станет кросс-тенантной утечкой.
- **Вывод:** централизовать `visible_messages` **сейчас** (маленький PR) — это и
  закрытие data-leak-риска, и подготовка org-фазы.

---

## Что НЕ пересеклось, но важно

- **ChatLive-монолит** (архитектура S1/W1) — чисто maintainability + операционный
  риск, security-следствий нет. Первая экстракция: панель тредов → мультиселект →
  композер. Не срочно, но копится.
- **Rate-limit на логин** (security P2-1) — самостоятельная defense-in-depth,
  приемлемо при ~20 юзерах, поднять при росте.

---

## Предлагаемые issues (ждут подтверждения перед созданием)

Ничего не создаю без явного «ок». Приоритеты по рубрике `CLAUDE.md`.

| # | Заголовок | Приоритет | Источник | Заметка |
|---|---|---|---|---|
| A | Медиа-декод/ffprobe/сжатие увести с request-пути в `:media`-воркер + ужать пиксельные лимиты | **P1** | security P1-1/2/3, arch S3, ⨯1 | одна PR закрывает 3 находки; шов готов |
| B | Смена пароля + «выйти на всех устройствах» + admin reset-ссылки (общий `Eden.Tokens`) | **P2** | security P2-2, ADR-0002 D6/D7, arch W2, ⨯3 | один заход |
| C | Централизовать фильтр видимости сообщений (`visible_messages` — единственный источник) | **P2** | arch S4, ⨯4 | + подготовка org-фазы; маленький PR |
| D | `Eden.Notifications` + Web-адаптер (централизация fan-out) | **P2** | arch S5, ADR-0001, ⨯2 | первый шаг уже принятого ADR |
| E | Гейт broadcast в `mark_read` на членство (read-receipt spoofing) | **P2** | security P2-3 | одна строка |
| F | Rate-limit на логин и инвайт-эндпоинт | **P2** | security P2-1/P3-2 | `plug_attack`/hammer |
| G | Экстракция панели тредов из `chat_live.ex` | **P2** | arch W1/S1 | по лекалам album_layout/rail_hook; первая из серии |
| H | Пачка P3-гигиены (filename-escape, token-прунинг, `try/after` на постере, строгий guard, HSTS preload) | **P3** | security P3-1/3/9/10/11 | одним чек-листом |
| I | retitle/коммент #177 — auth-направление заменено ADR-0002 | — | auth-трек | правка issue, gated |

**Рекомендованный порядок:** A (риск на single-VPS) → C и E (маленькие, закрывают
data-leak и spoof) → B и D (совпадают с ADR-0002/0001) → F → G → H. #177 (I) —
после того как покажешь ADR-0002 лиду.

---

## Артефакты ревью

- `docs/adr/0002-auth-model.md` — модель аутентификации (invite-gated, eden = SoR).
- `CONTEXT.md` — стартовый глоссарий домена (auth + мессенджинг).
- `docs/review/architecture-review-2026-07-03.html` — deep-modules разбор 5 зон.
- `docs/review/security-findings-2026-07-03.md` — P0–P3 + покрытие.
- `docs/review/consolidation-2026-07-03.md` — этот файл.
